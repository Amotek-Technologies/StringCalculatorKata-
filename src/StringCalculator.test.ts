const add = (input:string) => {
    if(''){
        return 0
    } 
    
}

describe('StringCalculator', () => {
  it("empty input", () => {
    // Arrange
  

    // Act
 

    // Assert
    expect(add("")).toBe(0)
  })
})
